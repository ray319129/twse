# 資料處理
pandas>=2.0,<3.0
numpy>=1.24,<2.0
pyarrow>=14.0

# HTTP / 解析
requests>=2.31
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=4.9

# 股價資料來源
yfinance>=0.2.40

# 設定 / Email 範本
PyYAML>=6.0
Jinja2>=3.1

# 時間處理
python-dateutil>=2.8
pytz>=2024.1

# 新聞事件 AI 分類(Claude Haiku;無 ANTHROPIC_API_KEY 時自動略過,不影響其餘流程)
anthropic>=0.40

# Discord 通知裡的日K縮圖(scripts/alert_chart.py)。
# 只在盤中通知時用到,且 import 失敗會自動略過畫圖 —— 不會讓盯盤壞掉。
matplotlib>=3.8

# K線型態辨識(scripts/tech_signals.py,61 種 CDL* 型態)。
# 0.5.0 起 PyPI 直接有預編 wheels,不再需要先裝 C 函式庫;import 失敗會自動略過型態。
TA-Lib>=0.5
