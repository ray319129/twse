{
  "framework": null,
  "outputDirectory": "docs",
  "functions": {
    "api/health.py": {
      "maxDuration": 60
    },
    "api/detail.py": {
      "maxDuration": 60
    },
    "api/quote.py": {
      "maxDuration": 15
    },
    "api/portfolio_ocr.py": {
      "maxDuration": 60
    },
    "api/portfolio_review.py": {
      "maxDuration": 60
    },
    "api/watchlist.py": {
      "maxDuration": 20
    },
    "api/daytrade.py": {
      "maxDuration": 20
    }
  },
  "ignoreCommand": "git rev-parse --verify -q HEAD^ >/dev/null 2>&1 || exit 1; git diff --name-only HEAD^ HEAD | grep -qvE '^(data/|docs/[^/]*\\.json$)' && exit 1 || exit 0"
}
