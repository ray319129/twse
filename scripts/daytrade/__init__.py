"""當沖(Day Trading)層 —— 與既有選股/信心分系統**完全隔離**的獨立模組。

## 為什麼要隔離(使用者 2026-09-08 明確要求「當沖功能要獨立出來測試」)

這一層跟 `scripts/quotes.py` 的鐵則一是同一個道理:當沖訊號是高頻、高成本、
未經驗證的東西,一旦它的邏輯滲進 `scoring` / `screener` / `backtest`,
既有那套「盤後選股 + 績效台帳」就再也分不清楚績效是誰造成的。

所以:
- **本模組不得被 `scoring` / `indicators` / `backtest` / `screener` / `main` import**
  (`scripts/check_realtime_isolation.py` 的 PROTECTED 清單已納入本模組)
- 自己的 config(`config/daytrade.yaml`)、自己的資料夾(`data/daytrade/`)、
  自己的 Discord 頻道、自己的台帳 —— 關掉它,既有系統一行都不受影響
- 台帳分開記:當沖的勝率/成本不能跟波段選股的台帳混在一起算

## 為什麼成本計算是這一層的地基

當沖來回成本 = 手續費×2 + 當沖證交稅 + **跨價差**。前兩項是固定的 0.321%,
第三項隨股價落在哪個 tick 級距而劇烈變動 —— 實測 99 元的股票來回 0.523%,
105 元的股票 1.273%,**同一套策略成本差 2.4 倍**。

所以這裡的順序是:先算得出成本,才有資格談訊號。見 `cost.py`。
"""
